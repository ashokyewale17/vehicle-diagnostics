# Example environment variables
NODE_ENV=production
PORT=3000

# For deployment (used by GitHub Actions)
# DOCKER_REGISTRY: e.g., ghcr.io or your ECR registry
# DOCKER_IMAGE: repository/name
# EC2_SSH_HOST: ec2-user@your.ec2.ip.address
# EC2_SSH_KEY: the private key (store in GitHub Secrets as SSH_PRIVATE_KEY)
